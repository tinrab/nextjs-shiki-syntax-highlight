const config = {
    plugins: [
        'prettier-plugin-tailwindcss',
    ],
    singleQuote: true,
    trailingComma: 'all',
    semi: true,
    arrowParens: 'always',
    tailwindFunctions: ['cn'],
};

export default config;
